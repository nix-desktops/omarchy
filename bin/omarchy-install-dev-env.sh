# Upstream installs a language globally with mise; here the menu's
# Install > Development entries scaffold a new project with that language's
# devShell instead (omarchy-dev-env). No argument: pick any template.
exec omarchy-dev-env "$@"
