# Upstream runs Omarchy's Arch first-login provisioning here (mise, Chromium
# and keyring setup, GNOME theme, …). On NixOS that setup is declarative;
# first-boot tasks belong to the welcome app.
exit 0
