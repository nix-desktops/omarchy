# The lock screen's PAM services (omarchy-lock-password, and
# omarchy-lock-fingerprint with fprintd) are declared in the NixOS config
# (the omarchy NixOS module), so there is nothing to write into /etc here.
exit 0
