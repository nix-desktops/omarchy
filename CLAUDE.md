# nix-desktops/omarchy

Omarchy on NixOS as a standalone flake. The full context, ground rules
(commit identity, no AI attribution, ask before pushing), gotchas and the
remaining work are in the handoff:

@HANDOFF.md

What the desktop installs by default, what belongs to the ecosystem switch
and what the configurator asks for is decided in:

@PACKAGES.md
